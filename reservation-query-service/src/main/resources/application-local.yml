spring:
  mongodb:
    uri: mongodb://localhost:27017/reservation_query

  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: reservation-query-group

  mail:
    host: smtp.gmail.com
    port: 587
    username: esanabriac@ucentral.edu.co
    password: kaai sfqb abqn krvm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8082

kafka:
  topic:
    reservation-events: hotel.reservation.events
